import 'package:flutter/material.dart';

class Cart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('郭炯韦')),
      body: Column(children: <Widget>[
        Text('购物车页面')
      ]),
    );
  }
}